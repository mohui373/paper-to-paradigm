# Keep repository text files consistent across Windows, macOS, and Linux.
* text=auto eol=lf

# Preserve conventional Windows line endings for shell-specific scripts.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Keep generated PDF fixtures out of text diffs and whitespace checks.
*.pdf binary
